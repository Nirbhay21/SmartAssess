'use client';

import RecruiterOnboardingClient from './RecruiterOnboardingClient';

// Server-side route that renders the client onboarding flow for recruiters
export default function Page() {
  return <RecruiterOnboardingClient />;
}
