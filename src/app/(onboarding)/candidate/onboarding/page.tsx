import CandidateOnboardingClient from '@/app/(onboarding)/candidate/onboarding/CandidateOnboardingClient';

// Server-side route that renders the client onboarding flow for candidates
export default function Page() {
  return <CandidateOnboardingClient />;
}
